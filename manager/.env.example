# Flint Manager — copy to .env and fill in values
#
#   cp .env.example .env

# ── Auth ──────────────────────────────────────────────────────────────────────
# Bearer token / cookie value that protects the manager UI.
#
# If this key is missing or set to the placeholder, the server auto-generates
# a cryptographically random key on first boot and saves it to .env.
# Copy the key from the console output, or set your own:
#   openssl rand -hex 32
MANAGER_API_KEY=change-me-to-a-strong-random-secret

# ── Server ────────────────────────────────────────────────────────────────────
MANAGER_PORT=4000

# ── Optional: default site path ───────────────────────────────────────────────
# If you only manage one site, set this and it becomes the default in the registry.
# DEFAULT_SITE_PATH=../

# ── Deploy targets (set in the SITE's .env, not here) ─────────────────────────
# The manager detects which targets are available by reading the site's .env.
# Add the relevant vars to <site>/.env to unlock each platform's Deploy button.
#
# Cloudflare Pages:
#   CLOUDFLARE_GLOBAL_API_KEY=<global api key>
#   CLOUDFLARE_ACCOUNT_ID=<account-id>
#   CF_PAGES_PROJECT=<project-name>
#
# Vercel:
#   VERCEL_TOKEN=<token>
#
# Netlify:
#   NETLIFY_AUTH_TOKEN=<token>
#   NETLIFY_SITE_ID=<site-id>
#
# GitHub Pages:
#   GH_TOKEN=<personal-access-token>
#   GH_REPO=<owner/repo>
